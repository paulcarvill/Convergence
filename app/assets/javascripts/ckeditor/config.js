CKEDITOR.editorConfig = function( config ) {

	console.log(config);
    config.toolbar_convergence = [
	  ['Image'],
	  ['Bold','Italic'],
	  ['NumberedList','BulletedList'],
	  [ 'Link','Unlink','Anchor' ],
	  ['Source']
	]
};