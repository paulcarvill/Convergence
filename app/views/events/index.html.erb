<div id="container">
      
  <% @events.each do |event| %>
  <div class="heading">
    <h1><%= link_to event.name, event %></h1>
  </div>

  <div class="content">
    <img src="#"/>
    <div>
      <p><%= event.venue.name %></p>
      <p><%= event.start_at %></p>
      <p><%= event.end_at %></p>
      <p>all day: <%= event.all_day %></p>
      <p>£<%= event.price %></p>
    </div>
  </div>

  <% end %>
  
</div>