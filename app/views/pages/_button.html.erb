<% unless event.nil? %>
	<% if event.tickets %>
		<% event.tickets.each do |ticket| %>
			<p><a href="<%= ticket.url %>" class="buy"><%= ticket.name %> tickets</a></p>
		<% end %>
	<% end %>
<% end %>