<%= render "interactivepanel" %>

<%= render "carouselscript" %>

<%= render "eventsrow" %>

<%= render "blogrow" %>

<%= render "videorow" %>

<%= render "mailinglistrow" %>

<%= render "footer" %>

<%= render "interactive" %>

<script type="text/javascript">

$(document).ready(function() {
  $('nav').waypoint('sticky', {
    handler: function(dir) {
        stopDrawing = (dir == "down" ? true : false);
    }
  })
 });

</script>