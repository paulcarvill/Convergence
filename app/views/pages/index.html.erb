<%= render "interactivepanel" %>

<%= render "carouselscript" %>

<% if !params['launch'] %>
<%= render "eventsrow" %>
<% end %>

<%= render "blogrow" %>

<%= render "videorow" %>

<%# render "interactive" %>

<script type="text/javascript">

$(document).ready(function() {
  var b = $('body');
  $('nav').waypoint('sticky', {
    handler: function(dir) {
        b.toggleClass('adjust');
        stopDrawing = (dir == "down" ? true : false);
        if(dir =="down") {
        	$('canvas').first().css('display', 'none');
        } else {
			$('canvas').first().css('display', 'block');
        }
    },
    offset: 116
  })

  

 });

</script>